{% extends "base.html.twig" %}

{% block body %}
    <h1>test</h1>

    {{ form_start(form) }}

    {# {{ form_row(form.email) }} #}
    {# {{ form_row(form.submit) }} #}

    <button type="submit" class="btn btn-primary">Create</button>
    
    {{ form_end(form) }}



{% endblock %}
